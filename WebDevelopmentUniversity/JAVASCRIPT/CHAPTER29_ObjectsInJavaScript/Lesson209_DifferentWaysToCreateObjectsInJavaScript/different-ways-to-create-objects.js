let myObject = new Object();
let myObject2 = {}; // ! It's the most recommendable to use (simplified syntax)

let myString = new String("Hello");
let myString2 = "Hola"; // ! It's the most recommendable to use (simplified syntax)

let myNumber = new Number(1);
let myNumber2 = 1; // ! It's the most recommendable to use (simplified syntax)

let myBoolean = new Boolean(false);
let myBoolean2 = false; // ! It's the most recommendable to use (simplified syntax)

let myArray = new Array();
let myArray2 = []; // ! It's the most recommendable to use (simplified syntax)

let myFunction = new Function();
let myFunction2 = function(){}; // ! It's the most recommendable to use (simplified syntax)